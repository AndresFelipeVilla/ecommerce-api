services:

  app:
    build:
      target: production
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: prod
    restart: always

  postgres:
    restart: always
    # Puertos ocultos por seguridad en producción
