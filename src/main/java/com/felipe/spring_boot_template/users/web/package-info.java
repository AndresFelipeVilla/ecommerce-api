/**
 * Capa <strong>web</strong> del módulo Users.
 *
 * <p>
 * Contiene los controladores REST ({@code UserController}) que exponen
 * los endpoints HTTP del módulo. Este paquete es
 * <strong>package-private</strong>:
 * los controladores no deben ser referenciados directamente por otros módulos;
 * son el punto de entrada HTTP, no una API entre módulos.
 * </p>
 */
package com.felipe.spring_boot_template.users.web;
